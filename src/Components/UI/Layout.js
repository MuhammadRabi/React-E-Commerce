const Layout = ({ children }) => {
  return (
    <main className="flex justify-center items-center w-full py-14 px-3 md:px-10 lg:px-20 bg-slate-100">
      {children}
    </main>
  )
}
export default Layout
